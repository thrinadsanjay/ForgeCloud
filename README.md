# ForgeCloud

Self-service portal — **Forge**.

## Docs

- [Catalog & SSO admin guide](docs/admin-catalog-guide.md) — Packages, App roles, Hostname sync, OIDC presets, and validation rules.
- Design notes under `docs/superpowers/specs/` (application package roles, Ansible bootstrap).

## Quick start

See `docker-compose.yml` and `.env.example`. Default UI: `http://localhost:4100`.
