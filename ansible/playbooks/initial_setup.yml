---
# Forge guest first-boot / post-clone setup (Linux only).
# Invoked from the Forge container as Ansible controller.
- name: Forge initial setup
  hosts: guests
  become: true
  gather_facts: true
  roles:
    - role: initial_setup
